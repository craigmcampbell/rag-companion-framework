name: CI (PR Fast)

on:
  pull_request:
  push:
    branches: [main]

jobs:
  middleware-fast:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          python -m venv .venv
          .venv/bin/pip install -r requirements.txt
          .venv/bin/pip install basedpyright pyyaml

      - name: Typecheck middleware
        run: |
          # Error-level only: basedpyright treats warnings as nonzero exit by default;
          # middleware still has many strictness warnings (reportAny, etc.).
          # pyrightconfig.json expects .venv at repo root — create it above, run via that interpreter.
          .venv/bin/python -m basedpyright services/middleware --level error

      - name: Run tier 1 validation
        run: |
          .venv/bin/python services/middleware/validate.py --tier 1

      - name: Validate Taskfile YAML parses
        run: |
          .venv/bin/python - <<'PY'
          import pathlib
          import yaml

          taskfile = pathlib.Path("Taskfile.yml")
          yaml.safe_load(taskfile.read_text())
          print("Taskfile.yml OK")
          PY
